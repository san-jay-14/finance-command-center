VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_USER_ID=
# Publisher Login app (not the Trading API app) — see
# https://smartapi.angelone.in/publisher-login. Angel One requires an HTTPS
# redirect_url (no localhost/HTTP), so this must point at a real deployment,
# e.g. https://<your-domain>/connect/callback.
VITE_ANGEL_ONE_API_KEY=
VITE_ANGEL_ONE_REDIRECT_URL=
